import { styleDefaults } from "../../theme";

const appStyle = (theme) => ({
  flagIcon: {
    width: "60px",
    height: "auto",
  },
  visuallyHidden: styleDefaults.visuallyHidden,
});

export default appStyle;
